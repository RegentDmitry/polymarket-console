# Quick Start Guide

Быстрая шпаргалка по установке и запуску ботов.

## 📦 Установка (один раз)

```bash
cd earthquakes
bash install.sh
```

Скрипт автоматически:
- Создаст `.venv`
- Установит все зависимости
- Проверит Claude Code CLI
- Создаст `.env` из шаблона

⚠️ **После установки:** отредактируйте `.env` и добавьте API credentials!

```bash
nano .env
```

## 🚀 Запуск ботов

### Интерактивное меню (проще всего)

```bash
bash start.sh
```

Меню покажет:
1. Trading Bot (dry-run) - безопасно, без реальных сделок
2. Trading Bot (live, confirm) - реальные сделки с подтверждением
3. Trading Bot (live, auto) - автоматическая торговля
4. Update Bot - обновление списка рынков
5. Exit

### Прямой запуск

**Trading Bot (тестирование):**
```bash
bash run_trading_bot.sh
```

**Trading Bot (реальная торговля):**
```bash
bash run_trading_bot.sh --live              # С подтверждением каждой сделки
bash run_trading_bot.sh --live --auto       # Автоматически (осторожно!)
```

**Trading Bot с параметрами:**
```bash
bash run_trading_bot.sh --interval 1m --min-edge 0.02 --min-apy 0.50
```

**Update Bot:**
```bash
bash run_update_bot.sh                      # Каждые 6 часов
bash run_update_bot.sh --interval 12h       # Каждые 12 часов
```

## ⌨️ Горячие клавиши в TUI

### Trading Bot:
- **Q** - выход
- **R** - запустить сканирование
- **H** - показать статистику
- **M** - переключить режим AUTO/CONFIRM
- **Y/N** - подтвердить/отклонить сделку (в режиме CONFIRM)

### Update Bot:
- **Q** - выход
- **R** - запустить обновление немедленно

## 📁 Важные файлы

```
.env                         # API credentials (создайте из .env.example)
earthquake_markets.json      # Конфигурация рынков
trading_bot/data/            # Данные позиций и логи
```

## 🔧 Устранение проблем

**Скрипт не запускается (`cannot execute`):**
```bash
# Используйте bash явно
bash install.sh
bash start.sh
```

**Ошибки с line endings (`\r command not found`):**
```bash
# Конвертируйте файл
sed -i 's/\r$//' install.sh
sed -i 's/\r$//' start.sh
```

**Claude Code не найден:**
```bash
# Установите Claude Code CLI
npm install -g @anthropic-ai/claude-code
```

**На WSL работает медленно:**
```bash
# Переместите проект на Linux FS
cp -r /mnt/c/github/polymarket_console/earthquakes ~/earthquakes
cd ~/earthquakes
bash install.sh
```

## 📖 Документация

- [README.md](README.md) - полная документация
- [trading_bot/README.md](trading_bot/README.md) - торговый бот
- [update_bot/README.md](update_bot/README.md) - update бот

## ⚠️ Безопасность

**Режимы торговли:**
- **DRY RUN** (по умолчанию) - безопасно, только показывает сигналы
- **CONFIRM** (`--live`) - реальные сделки с подтверждением
- **AUTO** (`--live --auto`) - автоматически, БЕЗ подтверждений ⚠️

**Всегда начинайте с DRY RUN!**

## 🎯 Типичный workflow

1. **Первый запуск:**
   ```bash
   bash install.sh
   nano .env  # Настройте API credentials
   ```

2. **Тестирование (безопасно):**
   ```bash
   bash run_trading_bot.sh
   # Понаблюдайте за сигналами и логикой
   ```

3. **Live торговля с подтверждением:**
   ```bash
   bash run_trading_bot.sh --live
   # Подтверждайте каждую сделку вручную (Y/N)
   ```

4. **Update bot в фоне:**
   ```bash
   # В отдельном терминале
   bash run_update_bot.sh
   # Обновляет earthquake_markets.json каждые 6 часов
   ```

5. **Автоматическая торговля (когда уверены):**
   ```bash
   bash run_trading_bot.sh --live --auto
   # ⚠️ Будет торговать автоматически!
   ```
